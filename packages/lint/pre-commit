#!/usr/bin/env node
const childProcess = require('child_process');

try {
  const lint = require.resolve('@magijs/lint/bin');
  childProcess.fork(lint, [], {
    stdio: 'inherit',
  });
} catch (error) {
  console.log(error.stdout.toString());
  process.exit(1);
}
